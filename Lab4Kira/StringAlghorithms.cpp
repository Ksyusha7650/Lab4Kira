#include "StringAlghotithms.h"



void findSimilarSequences(Lines& text) {
	Lines lineSequence;
	Letters sequence, tempSequence;
	int sizeSequence = 0;

	for (int i = 0; i < text.getSize(); i++)
	{
		//lineSequence[i] = text[i];
		//sizeSequence++;
		//sequence.setSize(sizeSequence);
		//tempSequence[i] = text[i];
		//sequence[sizeSequence - 1] = lineSequence[i];
	}
}